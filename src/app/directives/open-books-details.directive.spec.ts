import { OpenBooksDetailsDirective } from './open-books-details.directive';

describe('OpenBooksDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenBooksDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
